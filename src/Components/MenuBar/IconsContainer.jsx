export const    IconsContainer = ({ children }) => {
	return <div className="flex w-full lg:w-fit h-full">
		<div className="flex lg:flex-col mb-0 mt-auto asbolute sticky bottom-0 justify-center w-full h-full">
			{children}
		</div>
	</div>
}